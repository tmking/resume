---
data_sources:
- items_root: /
  layouts_root: /
  type: filesystem_unified
- items_root: /static
  type: static

output_dir: output

deploy:
  default:
    kind: rsync
    dst: "pla.to:/home/lordzork/vhosts/resume.pla.to"
    options: [ '-gpPrvz', "--delete-after" ]